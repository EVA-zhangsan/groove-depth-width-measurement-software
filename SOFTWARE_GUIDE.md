槽型深度宽度测量软件 v1.0.3

【启动】
双击“槽型深度宽度测量软件.exe”，软件将直接进入测量系统，无需打开 Python、CMD、PowerShell 或浏览器。

【图像序列测量】
1. 在左侧填写样本编号、槽型、标准槽深、标准槽宽、允许误差和操作人员。
2. 点击“应用任务参数”。
3. 点击“导入图像序列”，选择包含连续激光图像的文件夹。
4. 软件自动完成图像读取、激光中心提取、三维点云重建和槽深槽宽分析。
5. 右侧可查看原始图像、中心提取、二值掩膜及测量结果；中间区域可交互查看三维点云。
6. 点击“生成 PDF 报告”保存本次测量结果。

【点云测量】
点击“导入点云文件”，支持 CSV、PLY、PCD、XYZ 格式。导入后可直接进行槽型分析、三维显示和报告输出。

【随包测试数据】
- data/测试数据_A_复杂工况：61 帧结构光软件测试图像，包含背景噪声、光强波动、轻微条纹抖动、槽位置漂移和尺寸微扰，用于检验处理流程在非理想输入下的稳定性。
- data/测试数据_B_基础工况：基础测试图像，用于快速流程复核与备用。
两组数据与外部图像序列使用完全相同的“导入图像序列”入口和处理流程；图像帧数由实际导入数据决定。

【判定口径】
界面和 PDF 报告中的“均值判定”，以多个有效截面的槽深/槽宽测量均值与任务标准值的偏差是否超过允许误差为依据。最小值、最大值及其位置用于描述局部波动，不直接参与均值判定。

【输出结果】
测量结果、历史记录和 PDF 报告保存在软件目录下的 outputs 文件夹。点击“打开输出结果”可直接打开该目录。

【注意事项】
- 请复制并保留完整软件文件夹，不要只复制 exe。
- 外部图像序列按文件名排序处理。
- 随包测试数据用于软件流程与算法稳定性验证，不作为真实硬件计量精度认证依据。
- 实际毫米坐标与测量精度依赖相机、激光平面和扫描步距等正式标定参数，并应结合参考样本和重复性验证进行复核。
